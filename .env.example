# Firebase configuration is handled via GoogleService-Info.plist (iOS)
# Download from Firebase Console > Project Settings > Your Apps > iOS
# Place at the project root as GoogleService-Info.plist
#
# No environment variables are required for MVP.
